import 'package:flutter/material.dart';

class LinkSended extends StatelessWidget {
  const LinkSended({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Email has been sent to your mail..."),
    );
  }
}